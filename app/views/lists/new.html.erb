<%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit 'create a list', class: 'btn btn-success mt-2' %>
  <% end %>
<%= link_to 'back to lists', lists_path %>
