<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.input :photo, as: :file %>
  <%= f.submit 'add a movie', class: 'btn btn-success mt-2' %>
<% end %>
<%= link_to 'back to lists', list_path(@list) %>
